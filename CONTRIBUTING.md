# Contributing

1. Create a focused branch.
2. Keep provider, application, and orchestration concerns separate.
3. Add tests for behavior changes.
4. Run `python -m pytest -q`, compilation, and the secret scan.
5. Never commit `.env`, credentials, generated caches, or private data.

